{
 "VaultSet": {
  "spec": "defi/xls-0065.md",
  "conds": [
   "The `VaultID` field is zero. (`temMALFORMED`)",
   "The `Data` field, if provided, is empty or exceeds 256 bytes. (`temMALFORMED`)",
   "The `AssetsMaximum` field, if provided, is negative. (`temMALFORMED`)",
   "None of `Data`, `AssetsMaximum`, or `DomainID` are provided (nothing to update). (`temMALFORMED`)"
  ]
 },
 "VaultDelete": {
  "spec": "defi/xls-0065.md",
  "conds": [
   "The `VaultID` field is zero. (`temMALFORMED`)",
   "- `SingleAssetVault`: The `MemoData` field is present. (`temDISABLED`)",
   "- `LendingProtocolV1_1`: The `MemoData` field is present and is empty or longer than 256 bytes. (`temMALFORMED`)"
  ]
 },
 "VaultDeposit": {
  "spec": "defi/xls-0065.md",
  "conds": [
   "The `VaultID` field is zero. (`temMALFORMED`)",
   "The `Amount` field is zero or negative. (`temBAD_AMOUNT`)"
  ]
 },
 "VaultWithdraw": {
  "spec": "defi/xls-0065.md",
  "conds": [
   "The `VaultID` field is zero. (`temMALFORMED`)",
   "The `Amount` field is zero or negative. (`temBAD_AMOUNT`)",
   "The `Destination` field, if provided, is zero. (`temMALFORMED`)"
  ]
 },
 "LoanBrokerSet": {
  "spec": "defi/xls-0066.md",
  "conds": [
   "`VaultID` is zero. (`temINVALID`)",
   "`Data` field is present, non-empty, and exceeds 256 bytes. (`temINVALID`)",
   "`ManagementFeeRate` is outside valid range (0 to 10000). (`temINVALID`)",
   "`CoverRateMinimum` is outside valid range (0 to 100000). (`temINVALID`)",
   "`CoverRateLiquidation` is outside valid range (0 to 100000). (`temINVALID`)",
   "`DebtMaximum` is negative or exceeds maximum allowed value. (`temINVALID`)",
   "One of `CoverRateMinimum` and `CoverRateLiquidation` is zero, and the other one is not. (Either both are zero, or both are non-zero) (`temINVALID`)",
   "`LoanBrokerID` is specified and is zero. (`temINVALID`)",
   "`LoanBrokerID` is specified and the submitter is attempting to modify fixed fields (`ManagementFeeRate`, `CoverRateMinimum`, `CoverRateLiquidation`). (`temINVALID`)"
  ]
 },
 "LoanBrokerDelete": {
  "spec": "defi/xls-0066.md",
  "conds": [
   "`LoanBrokerID` is zero. (`temINVALID`)"
  ]
 },
 "LoanBrokerCoverDeposit": {
  "spec": "defi/xls-0066.md",
  "conds": [
   "`LoanBrokerID` is zero. (`temINVALID`)",
   "`Amount <= 0`. (`temBAD_AMOUNT`)",
   "`Amount` is not a legal net amount. (`temBAD_AMOUNT`)"
  ]
 },
 "LoanBrokerCoverWithdraw": {
  "spec": "defi/xls-0066.md",
  "conds": [
   "`LoanBrokerID` is zero. (`temINVALID`)",
   "`Amount <= 0` or is not a legal net amount. (`temBAD_AMOUNT`)",
   "`Destination` is specified and is zero. (`temMALFORMED`)"
  ]
 },
 "LoanBrokerCoverClawback": {
  "spec": "defi/xls-0066.md",
  "conds": [
   "Neither `LoanBrokerID` nor `Amount` are specified. (`temINVALID`)",
   "`LoanBrokerID` is specified and is zero. (`temINVALID`)",
   "`Amount` is specified and `Amount < 0`. (`temBAD_AMOUNT`)",
   "`Amount` specifies an XRP amount. (`temBAD_AMOUNT`)",
   "`Amount` is specified and is not a legal net amount. (`temBAD_AMOUNT`)",
   "`LoanBrokerID` is not specified and `Amount` specifies an MPT. (`temINVALID`)",
   "`LoanBrokerID` is not specified, `Amount` specifies an IOU, and `Amount.issuer` is the submitter `Account` or zero. (`temINVALID`)"
  ]
 },
 "LoanSet": {
  "spec": "defi/xls-0066.md",
  "conds": [
   "`LoanBrokerID` is specified and is zero. (`temINVALID`)",
   "`CounterpartySignature` is not present and the transaction is not part of a `Batch` inner transaction. (`temBAD_SIGNER`)",
   "The transaction is a `Batch` inner transaction and the `Counterparty` field is not specified. (`temBAD_SIGNER`)",
   "`CounterpartySignature` contains an invalid signing key. (`temBAD_SIGNER`)",
   "`Data` field is present, non-empty, and exceeds 256 bytes. (`temINVALID`)",
   "`LoanServiceFee`, `LatePaymentFee`, or `ClosePaymentFee` is negative. (`temINVALID`)",
   "`PrincipalRequested <= 0`. (`temINVALID`)",
   "`LoanOriginationFee` is negative or exceeds `PrincipalRequested`. (`temINVALID`)",
   "`InterestRate` exceeds maximum allowed value. (`temINVALID`)",
   "`OverpaymentFee` exceeds maximum allowed value. (`temINVALID`)",
   "`LateInterestRate` exceeds maximum allowed value. (`temINVALID`)",
   "`CloseInterestRate` exceeds maximum allowed value. (`temINVALID`)",
   "`OverpaymentInterestRate` exceeds maximum allowed value. (`temINVALID`)",
   "`PaymentTotal <= 0`. (`temINVALID`)",
   "`PaymentInterval` is less than `60` seconds. (`temINVALID`)",
   "`GracePeriod` is less than `60` seconds or greater than the `PaymentInterval`. (`temINVALID`)"
  ]
 },
 "LoanDelete": {
  "spec": "defi/xls-0066.md",
  "conds": [
   "`LoanID` is zero. (`temINVALID`)"
  ]
 },
 "LoanManage": {
  "spec": "defi/xls-0066.md",
  "conds": [
   "`LoanID` is zero. (`temINVALID`)",
   "More than one of `tfLoanDefault`, `tfLoanImpair`, or `tfLoanUnimpair` flags are set (flags are mutually exclusive). (`temINVALID_FLAG`)"
  ]
 },
 "LoanPay": {
  "spec": "defi/xls-0066.md",
  "conds": [
   "`LoanID` is zero. (`temINVALID`)",
   "`Amount <= 0`. (`temBAD_AMOUNT`)",
   "More than one of `tfLoanLatePayment`, `tfLoanFullPayment`, or `tfLoanOverpayment` flags are set (flags are mutually exclusive). (`temINVALID_FLAG`)"
  ]
 },
 "EscrowCreate": {
  "spec": "payments/xls-0100.md",
  "conds": [
   "The combination of `FinishAfter`, `Condition`, `CancelAfter`, and `Bytecode` is not one of the allowed combinations in the table above \u2014 e.g., none of `FinishAfter`/`Condition`/`Bytecode` is present (`temMALFORMED`).",
   "`Bytecode` is included but `CancelAfter` is not (`temBAD_EXPIRATION`).",
   "`Data` is included but `Bytecode` is not (`temMALFORMED`).",
   "`Data` exceeds the maximum allowed size (`temMALFORMED`).",
   "`Bytecode` is empty, or exceeds `BytecodeSizeLimit` (`temMALFORMED`).",
   "`Bytecode` is not valid WASM, or does not export an `escrow_finish` function with the required signature (`temINVALID_BYTECODE`).",
   "Smart Escrows are temporarily disabled by fee voting \u2014 i.e., `GasLimit` or `BytecodeSizeLimit` has been voted to `0` (`temTEMP_DISABLED`)."
  ]
 },
 "MPTokenIssuanceSet": {
  "spec": "tokens/xls-0099.md",
  "conds": [
   "`IssuerEncryptionKey`, `AuditorEncryptionKey`, or `tfMPTSetCanHoldConfidentialBalance` is present but the `ConfidentialTransfer` amendment is not enabled. (`temDISABLED`)",
   "`IssuerEncryptionKey` is present but is not exactly 33 bytes or is not a well-formed compressed secp256k1 point. (`temMALFORMED`)",
   "`AuditorEncryptionKey` is present but is not exactly 33 bytes or is not a well-formed compressed secp256k1 point. (`temMALFORMED`)",
   "`Holder` is present together with `IssuerEncryptionKey` or `AuditorEncryptionKey`. (`temMALFORMED`)"
  ]
 },
 "ConfidentialMPTMirrorUpdate": {
  "spec": "tokens/xls-0099.md",
  "conds": [
   "Either the `ConfidentialMPTKeyRotation` or the `ConfidentialTransfer` amendment is not enabled. (`temDISABLED`)",
   "Issuer mode: `Holder` is present but `Account` is not the issuer of the `MPTokenIssuanceID`. (`temMALFORMED`)",
   "Issuer mode(`Holder` is present): `Account` is the same as `Holder`. (`temMALFORMED`)",
   "Holder mode(`Holder` is absent): `Account` is the issuer. (`temMALFORMED`)",
   "Neither `IssuerEncryptedAmount` nor `AuditorEncryptedAmount` is present. (`temMALFORMED`)",
   "Any present `IssuerEncryptedAmount` or `AuditorEncryptedAmount` has an invalid length or represents an invalid elliptic curve point. (`temBAD_CIPHERTEXT`)",
   "`ZKProof` length is not exactly the expected proof size for the detected mode. (128 bytes in every mode - see Section 11) (`temMALFORMED`)"
  ]
 },
 "ConfidentialMPTHolderKeyUpdate": {
  "spec": "tokens/xls-0099.md",
  "conds": [
   "Either the `ConfidentialMPTKeyRotation` or the `ConfidentialTransfer` amendment is not enabled. (`temDISABLED`)",
   "Neither `tfHolderKeyRotation`, `tfHolderKeyRecovery`, nor `tfCancelRecovery` is set, or more than one is set. (`temINVALID_FLAG`)",
   "Account is the issuer of `MPTokenIssuanceID` - the issuer cannot hold confidential balances. (`temMALFORMED`)",
   "Rotation or recovery mode: `HolderEncryptionKey` is absent, is not exactly 33 bytes, or is not a well-formed compressed secp256k1 point. (`temMALFORMED`)",
   "Rotation mode: `ConfidentialBalanceSpending` or `ConfidentialBalanceInbox` is missing. (`temMALFORMED`)",
   "Recovery or cancel mode: `ConfidentialBalanceSpending` or `ConfidentialBalanceInbox` is present. (`temMALFORMED`)",
   "Cancel mode: `HolderEncryptionKey` or `ZKProof` is present - cancel mode requires no additional fields beyond `TransactionType`, Account, `MPTokenIssuanceID`, and Flags. (`temMALFORMED`)",
   "Any present `ConfidentialBalanceSpending` or `ConfidentialBalanceInbox` has an invalid length or represents an invalid elliptic curve point. (`temBAD_CIPHERTEXT`)",
   "Rotation or recovery mode: `ZKProof` is absent or its length is not exactly the expected size for the selected mode. (160 bytes in rotation mode, 64 bytes in recovery mode - see Section 11) (`temMALFORMED`)"
  ]
 },
 "ConfidentialMPTRecoverBalance": {
  "spec": "tokens/xls-0099.md",
  "conds": [
   "Either the `ConfidentialMPTKeyRotation` or the `ConfidentialTransfer` amendment is not enabled. (`temDISABLED`)",
   "Account is not the issuer of `MPTokenIssuanceID`. (`temMALFORMED`)",
   "Account is the same as Holder. (`temMALFORMED`)",
   "`ConfidentialBalanceSpending` has an invalid length or represents an invalid elliptic curve point. (`temBAD_CIPHERTEXT`)",
   "`ZKProof` is absent or its length is not exactly the expected compact Chaum-Pedersen equality proof size. (128 bytes - see Section 11) (`temMALFORMED`)"
  ]
 },
 "ConfidentialMPTConvert": {
  "spec": "tokens/xls-0096.md",
  "conds": [
   "The `ConfidentialTransfer` feature is not enabled on the ledger. (`temDISABLED`)",
   "`Account` is the Issuer. (`temMALFORMED`)",
   "`HolderEncryptionKey` is present but `ZKProof` is missing. (`temMALFORMED`)",
   "`HolderEncryptionKey` is absent but `ZKProof` is present. (`temMALFORMED`)",
   "The length of `HolderEncryptionKey` is not exactly 33 bytes. (`temMALFORMED`)",
   "The length of `ZKProof` is not exactly 64 bytes. (`temMALFORMED`)",
   "Any provided ciphertext (`Holder`, `Issuer`, or `Auditor`) has an invalid length or represents an invalid elliptic curve point. (`temBAD_CIPHERTEXT`)",
   "`MPTAmount` exceeds the maximum allowable MPT amount. (`temBAD_AMOUNT`)"
  ]
 },
 "ConfidentialMPTSend": {
  "spec": "tokens/xls-0096.md",
  "conds": [
   "The `ConfidentialTransfer` feature is not enabled on the ledger. (`temDISABLED`)",
   "The sender is the issuer of the MPT. (`temMALFORMED`)",
   "The destination is the issuer of the MPT. (`temMALFORMED`)",
   "The sender and destination accounts are the same. (`temMALFORMED`)",
   "The `ZKProof` is not exactly 946 bytes. (`temMALFORMED`)",
   "The `BalanceCommitment` or `AmountCommitment` is not a valid 33-byte compressed elliptic curve point. (`temMALFORMED`)",
   "Any required encrypted amount (`SenderEncryptedAmount`, `DestinationEncryptedAmount`, or `IssuerEncryptedAmount`) has an invalid length or represents an invalid elliptic curve point. (`temBAD_CIPHERTEXT`)",
   "The `AuditorEncryptedAmount` (if present) has an invalid length or represents an invalid elliptic curve point. (`temBAD_CIPHERTEXT`)"
  ]
 },
 "ConfidentialMPTMergeInbox": {
  "spec": "tokens/xls-0096.md",
  "conds": [
   "The `ConfidentialTransfer` feature is not enabled. (`temDISABLED`)",
   "The account submitting the transaction is the **Issuer**. (`temMALFORMED`)"
  ]
 },
 "ConfidentialMPTConvertBack": {
  "spec": "tokens/xls-0096.md",
  "conds": [
   "The `ConfidentialTransfer` feature is not enabled. (`temDISABLED`)",
   "The account submitting the transaction is the Issuer. (`temMALFORMED`)",
   "Ciphertext lengths or formats are invalid. (`temBAD_CIPHERTEXT`)",
   "`MPTAmount` is zero or greater than the maximum allowable supply. (`temBAD_AMOUNT`)",
   "The `BalanceCommitment` is not a valid 33-byte compressed elliptic curve point. (`temMALFORMED`)",
   "The `ZKProof` is not exactly 816 bytes. (`temMALFORMED`)"
  ]
 },
 "ConfidentialMPTClawback": {
  "spec": "tokens/xls-0096.md",
  "conds": [
   "The `ConfidentialTransfer` feature is not enabled. (`temDISABLED`)",
   "The `Account` is not the issuer of the `MPTokenIssuanceID`. (`temMALFORMED`)",
   "The `Account` is attempting to claw back from itself (`Account` == `Holder`). (`temMALFORMED`)",
   "The `ZKProof` is not exactly 64 bytes. (`temMALFORMED`)",
   "`MPTAmount` is zero or exceeds the maximum limits. (`temBAD_AMOUNT`)"
  ]
 },
 "MPTokenIssuanceCreate": {
  "spec": "tokens/xls-0090.md",
  "conds": [
   "`DomainID` is present but either `SingleAssetVault` or `PermissionedDomains` is disabled. (`temDISABLED`)",
   "`DomainID` is zero. (`temMALFORMED`)",
   "`DomainID` is present but `tfMPTRequireAuth` is not set. (`temMALFORMED`)"
  ]
 },
 "TokenPreauth": {
  "spec": "tokens/xls-0087.md",
  "conds": [
   "None or more than one of `Holder`, `Credentials`, and `DomainID` are included (`temMALFORMED`)",
   "Both `Currency` and `MPTokenIssuanceID` are included (`temMALFORMED`)",
   "The `Credentials` array is empty (`temMALFORMED`)",
   "The `Credentials` array has more than 8 credentials (`temMALFORMED`)"
  ]
 },
 "TrustSet": {
  "spec": "tokens/xls-0087.md",
  "conds": [
   "Both `CredentialIDs` and `DomainID` are included (`temMALFORMED`)"
  ]
 },
 "MPTokenAuthorize": {
  "spec": "tokens/xls-0087.md",
  "conds": [
   "Both `CredentialIDs` and `DomainID` are included (`temMALFORMED`)"
  ]
 },
 "TransactionProposalCreate": {
  "spec": "accounts/xls-0103.md",
  "conds": [
   "If present, `ProposedTransaction` is not a well-formed transaction of a known type (`temMALFORMED`). `ProposedTransaction` is required by the transaction format, so if it is missing, deserialization fails before preflight; submission returns a parse error rather than a transaction result, and no fee is charged.",
   "The proposed transaction fails the **stateless format checks (preflight) for its own transaction type**. These are the same checks it would receive if submitted directly, except checks that fail because a required signature is **absent** \u2014 such as XLS-56 \u00a72.3 rule 3.6 (a `Batch` missing a `BatchSigners` entry for a required participant) or XLS-66's check that a `LoanSet` outside a `Batch` carries a `CounterpartySignature` \u2014 which are skipped, since every signature slot is empty at creation by design (\u00a74.2.1). Checks that instead fail because a signature-bearing field is unexpectedly **present** still run unchanged: XLS-56 \u00a72.3 rules 2.5\u20132.7 continue to reject an inner transaction carrying `TxnSignature`, a non-empty `SigningPubKey`, or a `Signers` array (`temBAD_SIGNATURE`/`temBAD_REGKEY`/`temBAD_SIGNER`), enforcing that every inner transaction is unsigned per \u00a74.2.1. If any check fails, the proposal returns the `tem` code from that transaction type's preflight. Running these checks at creation is cheap and rejects malformed payloads immediately, instead of letting an invalid proposal gather signatures only to fail later. State-dependent (preclaim) checks are **not** run here; they are evaluated when the completed transaction is submitted.",
   "The proposed transaction is not unsigned. The result depends on the signature field and its contents:",
   "The proposed transaction cannot be independently submitted through the ordinary multi-sign path \u2014 it is itself a `TransactionProposalCreate`, `TransactionProposalSign`, or `TransactionProposalCancel`; or a pseudo-transaction (`EnableAmendment`, `SetFee`, `UNLModify`) (`temINVALID`).",
   "The proposed transaction carries the `tfInnerBatchTxn` flag. If `featureBatchV1_1` is disabled, this returns `temINVALID_FLAG`; otherwise, it returns `temINVALID_INNER_BATCH`.",
   "The proposed transaction does not specify `TicketSequence`, or specifies `Sequence` instead of or in addition to `TicketSequence` (`temSEQ_AND_TICKET`).",
   "`Expiration` is zero (`temBAD_EXPIRATION`). `Expiration` is required by the transaction format, so if it is missing, deserialization fails before preflight; submission returns a parse error rather than a transaction result, and no fee is charged."
  ]
 },
 "TransactionProposalSign": {
  "spec": "accounts/xls-0103.md",
  "conds": [
   "`ProposalID`, `SigningFor`, and `ProposalSignature` (with its `Account`, `SigningPubKey`, and `TxnSignature` sub-fields) are required by the transaction format, so if any is missing, deserialization fails before preflight; submission returns a parse error rather than a transaction result, and no fee is charged.",
   "`ProposalID` is present but malformed (`temMALFORMED`).",
   "`ProposalSignature.SigningPubKey` is not a well-formed public key (`temBAD_SIGNATURE`). Only the encoding is checked here. Whether that key is _authorized_ for `SigningFor`, and whether `TxnSignature` verifies, both need the ledger \u2014 the key has to be matched against a live regular key or `SignerList`, and the data the signature covers is the stored `ProposedTransaction`, which `TransactionProposalSign` does not carry."
  ]
 },
 "TransactionProposalCancel": {
  "spec": "accounts/xls-0103.md",
  "conds": [
   "`ProposalID` is required by the transaction format, so if it is missing, deserialization fails before preflight; submission returns a parse error rather than a transaction result, and no fee is charged. If present but malformed, returns `temMALFORMED`."
  ]
 },
 "SignerListSet": {
  "spec": "accounts/xls-0049.md",
  "conds": [
   "`TransactionTypeBitmask` is present but is not an array, contains elements that are not valid transaction type identifiers, or decodes to a malformed bitmask (implementation-defined `tem`-class error code)."
  ]
 },
 "SponsorshipSet": {
  "spec": "accounts/xls-0068.md",
  "conds": [
   "Both `CounterpartySponsor` and `Sponsee` are specified (`temMALFORMED`)",
   "Neither `CounterpartySponsor` nor `Sponsee` is specified (`temMALFORMED`)",
   "Both `tfSponsorshipSetRequireSignForFee` and `tfSponsorshipClearRequireSignForFee` are enabled (`temINVALID_FLAG`)",
   "Both `tfSponsorshipSetRequireSignForReserve` and `tfSponsorshipClearRequireSignForReserve` are enabled (`temINVALID_FLAG`)",
   "`CounterpartySponsor` is specified (which means that the `Sponsee` is submitting the transaction) and `tfDeleteObject` is not enabled, as only the counterparty sponsor can create/update the `Sponsorship` object (`temMALFORMED`)",
   "`MaxFee` is not denominated in XRP, or is negative (`temBAD_AMOUNT`)",
   "`FeeAmountDelta` is not denominated in XRP, or is zero (`temBAD_AMOUNT`)",
   "`CounterpartySponsor == Sponsee` (attempting to create self-sponsorship) (`temMALFORMED`)",
   "`RemainingOwnerCountDelta` is zero (`temINVALID`)",
   "No modification is requested at all \u2014 none of `FeeAmountDelta`, `MaxFee`, or `RemainingOwnerCountDelta` is present, and no `SponsorshipSet`-specific flags are set (`temREDUNDANT`). \"No flags set\" here refers only to the flags defined in [section 3.7.2](#372-flags); universal flags such as `tfFullyCanonicalSig` are ignored for this check.",
   "If `tfDeleteObject` is enabled:",
   "`FeeAmountDelta` is specified (`temMALFORMED`)",
   "`MaxFee` is specified (`temMALFORMED`)",
   "`RemainingOwnerCountDelta` is specified (`temMALFORMED`)",
   "`tfSponsorshipSetRequireSignForFee` is enabled (`temINVALID_FLAG`)",
   "`tfSponsorshipSetRequireSignForReserve` is enabled (`temINVALID_FLAG`)",
   "`tfSponsorshipClearRequireSignForFee` is enabled (`temINVALID_FLAG`)",
   "`tfSponsorshipClearRequireSignForReserve` is enabled (`temINVALID_FLAG`)"
  ]
 },
 "SponsorshipTransfer": {
  "spec": "accounts/xls-0068.md",
  "conds": [
   "Exactly one flag must be set (`temINVALID_FLAG`)",
   "The set of fields included in the transaction does not match the expected set for the specified flag. The exact error code depends on which field is misused: `Sponsor` present without `spfSponsorReserve` on the Create/Reassign path, or `SponsorFlags` present on the End path (`temINVALID_FLAG`); `Sponsee` present on the Create/Reassign path, `Sponsor` present on the End path, or `Sponsee` equal to `tx.Account` on the End path (`temMALFORMED`)",
   "(Create path) The `Sponsor` field or the `SponsorFlags` field is missing (`temMALFORMED`)",
   "(Create path) The `SponsorFlags` field does not include the `spfSponsorReserve` flag (`temINVALID_FLAG`)",
   "(Create path) `ObjectID` is not specified (account-level) and `SponsorSignature` is not included (`temMALFORMED`) \u2014 account-level creation always requires a co-signature; object-level creation may instead rely on a pre-funded `Sponsorship` object.",
   "(Reassign path) The `Sponsor` field or the `SponsorFlags` field is missing (`temMALFORMED`)",
   "(Reassign path) The `SponsorFlags` field does not include the `spfSponsorReserve` flag (`temINVALID_FLAG`)",
   "(Reassign path) `ObjectID` is not specified (account-level) and `SponsorSignature` is not included (`temMALFORMED`)"
  ]
 },
 "Payment": {
  "spec": "accounts/xls-0068.md",
  "conds": [
   "`tfNoRippleDirect`, `tfPartialPayment`, or `tfLimitQuality` are enabled (`temINVALID_FLAG`)",
   "`Amount` specifies a non-XRP currency (`temBAD_AMOUNT`)",
   "`SendMax` or `Paths` is included (`temINVALID`)"
  ]
 },
 "Batch": {
  "spec": "accounts/xls-0068.md",
  "conds": [
   "The outer `Batch` transaction includes `SponsorFlags.spfSponsorReserve` (`temINVALID_FLAG`).",
   "An inner transaction's `SponsorSignature.TxnSignature` is populated (`temBAD_SIGNATURE`).",
   "An inner transaction's `SponsorSignature.Signers` is populated (`temBAD_SIGNER`).",
   "An inner transaction's `SponsorSignature.SigningPubKey` is non-empty (`temBAD_REGKEY`).",
   "An inner transaction has `SponsorFlags.spfSponsorFee` enabled (`temINVALID_FLAG`).",
   "The outer transaction's `BatchSigners` array does not exactly match the set of sponsors required by the inner transactions (`temBAD_SIGNER`)."
  ]
 }
}